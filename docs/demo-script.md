# Unedited demo procedure

1. Use a consented/public image and show its path; do not show secrets.
2. Show `.env` variable names are configured without displaying values.
3. Run `python -m app.main --image path\to\consented-image.jpg` in one terminal.
4. Let the command complete without editing output or substituting URLs.
5. Open the printed Amoy explorer URL and show the transaction event and contract address.
6. Recompute the displayed canonical payload hash independently and show the matching public read-back. If the provider returns no social result or the chain fails, record that honest failure instead of retrying with fabricated data.
