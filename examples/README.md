# Local demo image

Place a consented or clearly public image at `examples/sample_input.jpg` before running the live pipeline. Image files in this directory are intentionally ignored so biometric source material is not published with the repository.
